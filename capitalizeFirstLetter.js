let carName = ["mercedes", "ford", "toyota"];
let capitalizedCarName = carName.map((carName)=> {
  return carName[0].toUpperCase() + carName.slice(1) });

  console.log(capitalizedCarName);
 alert(carName);
